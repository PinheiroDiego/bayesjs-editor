import { IStorage, IWeakStorage } from '../types';
export declare const createWeakStorage: <TKey extends object, TValue>(storage?: WeakMap<TKey, TValue> | undefined) => IWeakStorage<TKey, TValue>;
export declare const createStorage: <TKey, TValue>(storage?: Map<TKey, TValue> | undefined) => IStorage<TKey, TValue>;
