import { IClique, IGraph } from '../types';
export declare const bronKerbosch2Maker: (graph: IGraph, onFindClique: (clique: string[]) => void) => {
    bronKerbosch2: (R: string[], P: string[], X: string[]) => void;
};
export declare const findCliques: (graph: IGraph) => IClique[];
